{
  "name": "igunf",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "A local-first Instagram relationship review extension",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "check": "npm run typecheck && npm test && npm run build",
    "preview": "node scripts/preview.mjs"
  },
  "engines": {
    "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
  },
  "dependencies": {
    "fflate": "0.8.3"
  },
  "devDependencies": {
    "@types/chrome": "0.2.9",
    "@types/jsdom": "30.0.0",
    "@types/node": "22.20.2",
    "esbuild": "0.28.2",
    "jsdom": "30.0.1",
    "typescript": "7.0.2"
  }
}
